#include <iostream>

using namespace std;

int main()
{
    int a,i;
    cout<<"enter a number to find its factors";
    cin>>a;
    
    i=1;
    while(i<=a)
    {
        if(a%i==0)
        cout<<" "<<i;
        i++;
    }
    
    return 0;
}
